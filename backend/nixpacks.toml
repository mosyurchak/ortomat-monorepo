[phases.setup]
nixPkgs = ["nodejs-20_x", "npm"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build",
  "chmod +x start.sh",
  "ls -la dist/"
]

[start]
cmd = "npx prisma migrate deploy --schema=./prisma/schema.prisma && node dist/main.js"
