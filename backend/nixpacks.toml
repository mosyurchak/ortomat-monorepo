[phases.setup]
nixPkgs = ["nodejs-20_x", "npm"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build",
  "chmod +x entrypoint.sh",
  "ls -la dist/",
  "ls -la prisma/ | head -20"
]

[start]
cmd = "sh entrypoint.sh"
