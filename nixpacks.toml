[phases.setup]
nixPkgs = ['nodejs_20', 'npm-10_x']

[phases.install]
cmds = ['npm ci --legacy-peer-deps']

[phases.build]
cmds = ['cd backend && npm install --legacy-peer-deps && npx prisma generate && npm run build']

[start]
cmd = 'cd backend && npm run start:prod'