# 📖 Покрокова Інструкція: Як Створити Pull Request на GitHub

## Крок 1: Відкрийте GitHub Repository

1. Відкрийте браузер
2. Перейдіть на: **https://github.com/mosyurchak/ortomat-monorepo**
3. Увійдіть в свій GitHub акаунт (якщо ще не увійшли)

---

## Крок 2: Знайдіть вашу гілку

**Варіант А - Швидкий спосіб (якщо бачите банер):**

Зверху сторінки може з'явитись жовтий банер:
```
claude/explore-project-AmYdP had recent pushes less than a minute ago
[Compare & pull request]
```
➡️ **Натисніть на зелену кнопку "Compare & pull request"** і переходьте до Кроку 4.

---

**Варіант Б - Через меню (якщо банера немає):**

1. Знайдіть кнопку **"main"** (зверху зліва, біля назви репозиторію)
2. Натисніть на неї - відкриється випадаюче меню з гілками
3. В полі пошуку введіть: `claude/explore-project-AmYdP`
4. Натисніть на вашу гілку коли вона з'явиться

---

**Варіант В - Через Pull Requests:**

1. Натисніть на вкладку **"Pull requests"** (зверху)
2. Натисніть зелену кнопку **"New pull request"**
3. В полі **"compare:"** виберіть `claude/explore-project-AmYdP`
4. В полі **"base:"** залиште `main`

---

## Крок 3: Перевірте зміни

Ви побачите:
- **Кількість комітів:** "27 commits"
- **Кількість змінених файлів:** "34 files changed"
- **Зміни коду** (зелені + і червоні -)

**Перегляньте основні файли:**
- ✅ `backend/src/ortomats/ortomats.gateway.ts` - WebSocket оновлення
- ✅ `backend/src/admin/admin.service.ts` - Бекап система
- ✅ `frontend/src/pages/admin/settings.tsx` - UI для бекапу

**Переконайтесь що:**
- ✅ Base branch: `main`
- ✅ Compare branch: `claude/explore-project-AmYdP`
- ✅ "Able to merge" (зелена галочка)

---

## Крок 4: Створіть Pull Request

1. Натисніть зелену кнопку **"Create pull request"**

2. **Заповніть форму:**

### 📝 Title (заголовок):
```
Додано WebSocket оновлення статусів + бекап/відновлення + CRUD користувачів
```

### 📄 Description (опис):
Скопіюйте текст з файлу `PR_DESCRIPTION.md` який я створив:

**Швидка версія (якщо лінь багато писати):**
```markdown
## Основні зміни

✅ WebSocket Gateway - автоматичне оновлення статусів замовлень (pending → processing → completed)
✅ Система бекапу та відновлення даних через адмінпанель
✅ Monobank Payment інтеграція (заміна LiqPay)
✅ CRUD для лікарів та кур'єрів в адмінпанелі
✅ Маска телефону +38 (0XX) XXX-XX-XX
✅ 27 комітів з покращеннями

## Важливо після merge
Запустити на production:
```bash
npx prisma migrate deploy
```

Детальний опис див. у `PR_DESCRIPTION.md`
```

**Або повний опис:**
👉 Відкрийте файл `/home/user/ortomat-monorepo/PR_DESCRIPTION.md` та скопіюйте весь вміст

---

## Крок 5: Додаткові налаштування (опціонально)

### 👥 Reviewers (рецензенти):
- Якщо є колеги - додайте їх як reviewers
- Натисніть "Reviewers" справа → виберіть людину

### 🏷️ Labels (мітки):
- `enhancement` - для нових функцій
- `backend` - якщо є зміни в backend
- `frontend` - якщо є зміни в frontend

### 🎯 Milestone (етап):
- Якщо використовуєте - виберіть потрібний milestone

---

## Крок 6: Створіть PR

1. Прокрутіть вниз
2. Натисніть велику зелену кнопку **"Create pull request"**

✅ **Готово! PR створено!**

---

## Крок 7: Merge Pull Request (об'єднання з main)

Після створення PR, ви побачите кнопки:

### Варіанти merge:

**1. Merge pull request** (рекомендовано):
- Зберігає всі коміти в історії
- Створює merge commit
- ✅ Натисніть "Merge pull request" → "Confirm merge"

**2. Squash and merge:**
- Об'єднує всі 27 комітів в один
- Корисно для чистоти історії
- ⚠️ Використовуйте якщо хочете компактну історію

**3. Rebase and merge:**
- Застосовує коміти по черзі без merge commit
- ⚠️ Може бути складно при конфліктах

### 👉 Рекомендація: використовуйте "Merge pull request"

---

## Крок 8: Перевірка деплою на Railway

Після merge:

1. **Почекайте 30-60 секунд**
2. Зайдіть на **https://railway.app** → ваш проект
3. Розділ **"Deployments"** → подивіться новий деплой
4. Статус має бути: **"SUCCESS ✅"**

### Якщо деплой завершився успішно:

**Перевірте логи:**
```
✅ Starting container
✅ Installing dependencies
✅ Building application
✅ Prisma generate
✅ Starting server
```

**Важливо! Запустіть міграції:**

Railway консоль → виберіть backend service → Tab "Settings" → "Variables" → додайте команду:
```bash
npx prisma migrate deploy
```

Або через Railway CLI:
```bash
railway run npx prisma migrate deploy
```

---

## Крок 9: Тестування на Production

1. **Відкрийте сайт:** https://ortomat.com.ua
2. **Залогіньтесь як адмін:**
   - Email: `admin@ortomat.ua`
   - Password: (ваш пароль)

3. **Перевірте нові функції:**
   - ✅ Адмін → Налаштування → Бекап даних
   - ✅ Адмін → Користувачі → Створити лікаря (перевірте маску телефону)
   - ✅ Зробіть тестове замовлення → перевірте що статус змінюється
   - ✅ Адмін → Логи → подивіться нові логи WebSocket

---

## 🎉 Готово!

Якщо все працює - ваш PR успішно змержений і задеплоєний!

---

## ⚠️ Troubleshooting (якщо щось пішло не так)

### Проблема: "This branch has conflicts that must be resolved"

**Рішення:**
```bash
git checkout claude/explore-project-AmYdP
git pull origin main
# Вирішіть конфлікти в файлах
git add .
git commit -m "Resolve merge conflicts"
git push origin claude/explore-project-AmYdP
```

### Проблема: Railway деплой failed

**Перевірте логи:**
1. Railway → Deployments → клікніть на failed deployment
2. Подивіться помилки в логах
3. Найчастіші проблеми:
   - ❌ Prisma client не згенерований → додайте в build command: `prisma generate`
   - ❌ Environment variables відсутні → перевірте .env
   - ❌ Міграції не застосовані → запустіть `prisma migrate deploy`

### Проблема: Зміни не відображаються на сайті

**Можливі причини:**
1. ✅ Почекайте 2-3 хвилини (деплой займає час)
2. ✅ Очистіть кеш браузера (Ctrl+Shift+R)
3. ✅ Відкрийте в режимі інкогніто
4. ✅ Перевірте що frontend також задеплоївся (Vercel)

### Проблема: Не можу змержити PR (403 або permission denied)

**Рішення:**
- Попросіть власника репозиторію надати вам права "Write" або "Maintain"
- Або власник має змержити PR замість вас

---

## 📞 Потрібна допомога?

Якщо виникли проблеми - пишіть мені і я допоможу!

**Корисні посилання:**
- GitHub PR документація: https://docs.github.com/en/pull-requests
- Railway документація: https://docs.railway.app
- Prisma міграції: https://www.prisma.io/docs/concepts/components/prisma-migrate
